Reproduction environment
  GPU       : single CUDA GPU, ~16 GB+ (any recent NVIDIA card)
  Python    : 3.11
  Precision : float32, no mixed precision (the effective-logit-scale measurement and the
              softmax-collapse audit depend on the unmodified arithmetic)
  Seeds     : 12 per cell, seed0=0; bootstrap CIs use numpy default_rng(0), so intervals are deterministic
  Pinned deps in requirements.txt
